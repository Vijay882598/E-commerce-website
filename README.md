# MyApp

This project was generated with [Angular CLI](https://github.com/angular/angular-cli) version 18.2.21.

## Development server

Run `ng serve` for a dev server. Navigate to `http://localhost:4200/`. The application will automatically reload if you change any of the source files.

## Code scaffolding

Run `ng generate component component-name` to generate a new component. You can also use `ng generate directive|pipe|service|class|guard|interface|enum|module`.

## Build

Run `ng build` to build the project. The build artifacts will be stored in the `dist/` directory.

## Running unit tests

Run `ng test` to execute the unit tests via [Karma](https://karma-runner.github.io).

## Running end-to-end tests

Run `ng e2e` to execute the end-to-end tests via a platform of your choice. To use this command, you need to first add a package that implements end-to-end testing capabilities.

## Further help

To get more help on the Angular CLI use `ng help` or go check out the [Angular CLI Overview and Command Reference](https://angular.dev/tools/cli) page.

#Further help 
npm install -D tailwindcss postcss autoprefixer     tailwind install command
npx tailwindcss init                                tailwind congif file
npm install aos                                     AOS Package install command
npm i --save-dev @types/aos                         AOS Dependency Package install command

# 🛒 Mini E-Commerce Website (Angular 18)

A **Mini E-Commerce Website clone** built using **Angular 18**.  
This project demonstrates core e-commerce features such as category-based product browsing, cart management, dummy authentication, and smooth UI animations.

---

## 🚀 Project Overview

- Minimum **5-page** e-commerce application
- Developed using **Angular CLI v18**
- Clean and responsive UI
- Cart and authentication handled using **Local Storage**
- Inspired by real-world e-commerce platforms

---

## 🧩 Features

### 🏠 Home Page
- Landing page of the application
- Header includes a **Category Selection button**
- Users can select any category
- Redirects to **Category-wise Products Page**

---

### 📦 Products Page
- Displays products based on the selected category
- Each product card contains:
  - Product UI
  - **Add to Cart** button
  - **Buy Now** button

---

### 🛒 Cart Functionality
- **Add to Cart**
  - Adds the selected product to the cart
  - Redirects user to the **Cart Page**
- Cart data is stored in **Local Storage**
- Cart persists even after page refresh

---

### 💳 Buy Now & Payment Page
- Clicking **Buy Now** redirects to the **Payment Page**
- Simulates a real e-commerce checkout flow

---

### 🔐 Dummy Authentication
- Dummy login functionality implemented
- Login user data stored in **Local Storage**
- Used to simulate authentication behavior

---

## 🎨 UI & Animations

- **Tailwind CSS** for styling and responsive layout
- **AOS (Animate On Scroll)** for smooth animations
- Mobile-friendly and responsive design

---

## 🛠️ Technologies Used

- **Angular 18**
- **TypeScript**
- **Tailwind CSS**
- **AOS (Animate On Scroll)**
- **Local Storage**
- Dummy / Static API data

---

## 📦 Installation & Setup

### 🔧 Prerequisites
- Node.js (Latest LTS recommended)
- Angular CLI v18+

---

### 📥 Clone the Repository
```bash
git clone https://github.com/your-username/your-repo-name.git
cd your-repo-name







