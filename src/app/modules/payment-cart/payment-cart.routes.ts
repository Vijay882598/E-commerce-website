import { Routes } from "@angular/router";

import { PaymentCartComponent } from "./payment-cart.component";

export const PAYMENT_CART_ROUTES: Routes = [
    {
        path: '',
        component: PaymentCartComponent
    }
]